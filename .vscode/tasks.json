{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and run C++",
            "type": "shell",
            "command": "g++ -std=c++17 -D_GLIBCXX_DEBUG ${relativeFile} && ./a.out",
        }
    ]
}